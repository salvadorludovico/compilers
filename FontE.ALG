inicio
    varinicio
        x, y int;
        z real
        nome lit;
    varfim;
    
    leia x;
    se (x > 0) entao
        escreva "Positivo"
    fimse;
    
    y <- x + 5
    
    faca-ate (y < 10)
        escreva y;
        y <- y + 1;
    fimfaca;
    
fim